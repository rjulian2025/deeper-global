---
import type { AuthorityProfile } from '@/lib/authorities';

type Props = {
  authority: AuthorityProfile;
};

const { authority } = Astro.props;
---

<section class="section expertise-section" aria-labelledby="expertise-map-title">
  <div class="container">
    <div class="section-header">
      <div>
        <p class="eyebrow">Authority domains</p>
        <h2 id="expertise-map-title">{authority.expertiseSectionTitle}</h2>
        <p class="expertise-lede">
          Each domain reflects a clinical question Dr. Crenshaw helps clarify — not a generic service list.
        </p>
      </div>
    </div>
    <div class="expertise-map" role="list">
      {
        authority.expertiseDomains.map((domain) => (
          <button
            type="button"
            class="expertise-node card"
            role="listitem"
            data-authority-event="expertise_map_interaction"
            data-specialty={domain.label}
            data-destination={`#expertise-${domain.id}`}
            aria-describedby={`expertise-desc-${domain.id}`}
          >
            <span class="expertise-label">{domain.label}</span>
            <span class="expertise-desc" id={`expertise-desc-${domain.id}`}>{domain.description}</span>
          </button>
        ))
      }
    </div>
  </div>
</section>

<style>
  .expertise-section {
    background: var(--white);
  }

  .expertise-lede {
    margin: var(--space-4) 0 0;
    max-width: 36rem;
    color: var(--stone);
    line-height: var(--leading-relaxed);
  }

  .expertise-map {
    display: grid;
    gap: var(--space-4);
  }

  @media (min-width: 768px) {
    .expertise-map {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (min-width: 1024px) {
    .expertise-map {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }
  }

  .expertise-node {
    display: grid;
    gap: var(--space-3);
    text-align: left;
    cursor: pointer;
    font: inherit;
    width: 100%;
    min-height: 8.5rem;
    padding: var(--space-5) var(--space-6) var(--space-5) var(--accent-inset);
    border-left: var(--accent-bar-width) solid var(--ocean);
    transition: border-left-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s ease;
  }

  .expertise-node:hover,
  .expertise-node:focus-visible {
    border-left-color: var(--brass);
    box-shadow: var(--shadow);
    outline: none;
  }

  .expertise-label {
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-size: var(--text-md);
    font-weight: 600;
    color: var(--navy);
  }

  .expertise-desc {
    color: var(--stone);
    font-size: 0.875rem;
    line-height: var(--leading-relaxed);
  }

  @media (max-width: 640px) {
    .expertise-node {
      padding-left: calc(var(--space-5) + var(--accent-bar-gap));
    }
  }
</style>

<script>
  document.querySelectorAll('.expertise-node').forEach((node) => {
    node.addEventListener('click', () => {
      window.deeperTrackEvent?.('expertise_map_interaction', {
        content_type: 'authority',
        specialty: node.getAttribute('data-specialty') ?? undefined,
        destination: node.getAttribute('data-destination') ?? undefined,
      });
    });
  });
</script>
