---
/** @deprecated Use DiscoveryRow — retained for hub pages until Phase 3. */
import DiscoveryRow from '@/components/DiscoveryRow.astro';
import type { Question } from '@/lib/supabase';

type Props = {
  question: Question;
  headingLevel?: 2 | 3;
};

const { question, headingLevel = 2 } = Astro.props;
---

<DiscoveryRow question={question} headingLevel={headingLevel} />
