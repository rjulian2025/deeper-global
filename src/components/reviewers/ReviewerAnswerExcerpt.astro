---
import AnswerExcerpt from '@/components/AnswerExcerpt.astro';
import { REVIEWER_EXCERPT_MAX } from '@/lib/reviewer-page';
import type { Question } from '@/lib/supabase';

type Props = {
  question: Question;
};

const { question } = Astro.props;
---

<AnswerExcerpt question={question} maxLength={REVIEWER_EXCERPT_MAX} />
