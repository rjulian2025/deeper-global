# Sample fixtures (dry-run only)

These files validate the processor without touching `input/`.

```bash
python3 instantly-relaunch/working/process_leads.py --use-samples
```

Do not place real Instantly exports here. Use `input/old-campaign/` and `input/new-leads/` for production runs.
